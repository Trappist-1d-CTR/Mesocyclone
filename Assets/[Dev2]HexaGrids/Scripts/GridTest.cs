using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridTest : MonoBehaviour
{
    /*
    private HexaGrid TestGrid;

    // Start is called before the first frame update
    void Start()
    {
        /*
        TestGrid = new HexaGrid(4, 7, 0);

        TestGrid.PrintValues();

        TestGrid.Assign(0, 1, 0.98f);
        TestGrid.Assign(3, 0, 2.4f);

        TestGrid.PrintValues();

        Debug.Log("Position of [2, 5]: " + TestGrid.GetWorldPosition(2, 5));
        Debug.Log("Position of [2, 0]: " + TestGrid.GetWorldPosition(2, 0));
        Debug.Log("Position of [3, 1]: " + TestGrid.GetWorldPosition(3, 1));

        Debug.Log("Value of Position [3, 0]: " + TestGrid.valArray[TestGrid.GetCellID(3, 0).x, TestGrid.GetCellID(3, 0).y]);
    }
    */
}
